def add_binary2(a, b):
    '''
    add two postiive numbers and convert the sum to binary, using a built in function called bin()
    e.g 1+1 = 10
    '''
    sum = a+b
    return bin(sum).replace('0b','')
