
def toJadenCase(string):
    '''
    to display all the first letter in capital for each word
    '''
    
    final_word = []
    for word in string.split():
        final_word.append(word[0].title() + word[1::].lower())
    
    return ' '.join(final_word)
