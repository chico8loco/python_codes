def filter_list(l):
    '''
    create a function that takes a list of integers and strings and returns a new list with the strings removed
    '''
    
    filtered_lists = []
      
    for char in l:
        if isinstance(char, int): ## using isinstance to identify each item in a list
            filtered_lists.append(char)
    return filtered_lists
