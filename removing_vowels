def disemvowel2(string):
    '''
    function to remove the vowels in a phrase
    '''
    
    #phrase = string
    vowels = 'aeiou'  
    
    final_word = ''
    
    for x in string:
        if x.lower() not in vowels:
            final_word += x
    return final_word
    
